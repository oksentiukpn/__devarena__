<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

    <url>
        <loc>{{ url_for('main.home', _external=True) }}</loc>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>

    <url>
        <loc>{{ url_for('main.privacy_policy', _external=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.3</priority>
    </url>

    {% for user in users %}
    <url>
        <loc>{{ url_for('main.user_profile', username=user.username, _external=True) }}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.6</priority>
    </url>
    {% endfor %}

    {% for post in posts %}
    <url>
        <loc>{{ url_for('main.post_detail', post_id=post.id, _external=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.8</priority>
    </url>
    {% endfor %}

</urlset>
